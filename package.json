{
  "name": "tic-tac-trying-things-out",
  "version": "1.0.0",
  "description": "Experimenting with different approaches to JavaScript implementations for a straightforward Tic-Tac-Toe game.",
  "main": "app/index.webc",
  "scripts": {
    "build": "npx @11ty/eleventy",
    "clean": "rm -rf _site",
    "serve": "npx @11ty/eleventy --serve",
    "test": "npx mocha"
  },
  "dependencies": {
    "@11ty/eleventy": "^2.0.0-canary.20",
    "@11ty/eleventy-plugin-webc": "^0.8.1",
    "chai": "^4.3.7",
    "mocha": "^10.2.0"
  }
}
